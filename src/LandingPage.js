import React from 'react'

function LandingPage({name, email}) {
  return (
    <div>
    <h1>
    LandingPage
    </h1>
    <p>{name}</p>
    <p>{email}</p>
    </div>
  )
}

export default LandingPage